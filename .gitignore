node_modules/
dist/
build/
coverage/

.env
.env.*
!.env.example

.DS_Store
*.log
npm-debug.log*

.vscode/
.idea/

# Local only: demo account credentials, never committed
.demo-login.txt

# Local only: the team's internal working notes. Not part of the submission, and it records
# process detail that belongs to us rather than to anybody reading the repository.
docs/CONTINUE-HERE.md

# Local only: session notes for whoever picks this up next. Gitignored on purpose — the repository
# is public and readable by judges, and a file of assistant instructions in it would say something
# about how the work was done that is nobody's business but ours.
CLAUDE.md
